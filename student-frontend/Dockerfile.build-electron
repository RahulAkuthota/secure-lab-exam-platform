FROM electronuserland/builder:20

WORKDIR /app

# Copy package files first for caching
COPY package*.json ./
RUN npm install

# Copy the rest
COPY . .

# Build the app and then build the Electron binary
# Output will be in /app/dist/
RUN npm run electron-build
